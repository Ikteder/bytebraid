# Verification record — 2026-08-27

This record is completed from actual commands and observed output. It is not a benchmark claim.

## Environment

- Operating system: Windows
- Local compiler: GCC 16.2.0 from portable w64devkit 2.9.1
- Build mode: direct C++20 compile with optimization and strict warnings
- Fixture: 2.00 MiB across four eligible 512 KiB files plus one filtered note, generated by `tools/make_demo.py`

## Checks

| Check | Command | Result |
|---|---|---|
| Native tests | `build/bytebraid_tests.exe` | 3/3 passed: exact/near detection, empty directory, and invalid API inputs |
| Strict builds | `g++ -std=c++20 -O2 -Wall -Wextra -Wpedantic -Wconversion ...` | Test and CLI targets compiled with no diagnostics |
| Demo scan | `build/bytebraid.exe scan demo-corpus --min-size 1024 --similarity 0.70 --json demo-report.json` | 5 files observed, 4 analyzed, 1 exact group, 2 near pairs, 0 warnings |
| Exact evidence | demo JSON and terminal report | 524,288 conservative reclaimable bytes from one confirmed extra copy |
| Near evidence | demo JSON and terminal report | Both original/revision pairs scored 0.931034: 54 shared of 58 union chunks |
| JSON parse/assertions | Python standard-library `json` assertions | Passed for exact count, near count, and reclaimable bytes |
| Invalid CLI input | `build/bytebraid.exe scan demo-corpus --chunk-average 1000` | Rejected invalid chunk order and exited 2 |

An illustrative local demo scan took 413.9 ms in one PowerShell `Measure-Command` observation. This is environment-specific smoke-test timing, not a benchmark result.

## Limitations of this verification

The local run does not measure performance at large scale, compare other tools, test permission-denied paths, or test files changing during a scan. Public CI status will be recorded only after a verified GitHub Actions result exists.
